apiVersion: v1
kind: Service
metadata:
  labels:
    app: pages
    serviceFor: pages
  name: pages
  namespace: shoumya
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: pages
    serviceFor: pages
  type: NodePort
status:
  loadBalancer: {}
